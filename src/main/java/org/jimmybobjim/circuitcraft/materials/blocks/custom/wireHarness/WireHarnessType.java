package org.jimmybobjim.circuitcraft.materials.blocks.custom.wireHarness;

// once different variations of the wire harness are added, they will be added to this enum so the baked model stuff is easier
public enum WireHarnessType {
    WIRE_HARNESS,
    JUNCTION_BOX;
}
